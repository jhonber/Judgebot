#include<bits/stdc++.h>
using namespace std;

template <class T> string toStr(const T &x)
{ stringstream s; s << x; return s.str(); }

int main(){
/*	int n = 10;
	system("touch hack");
	for(int i=0;i<n;i++) system("echo 'hack' >> hack");

	for(int i=0;i<n; i++){
		char c[20] = "cp hack hack";
		char num[20] = "";
		sprintf(num,"%d",i);
		strcat(c,num);
		system(c);
	}
*/	
	system("touch cosita  /tmp/runs/writeme");
//	system("rm writeme");
	return 0;
}
